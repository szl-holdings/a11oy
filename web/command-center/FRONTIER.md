# Frontier board — honest zoom-out

Measured 2026-09-11. Doctrine v11. Λ = Conjecture 1.

This package is the inspectable Command Center replica under `web/command-center/`.
It is not a second flagship.

## Estate (do not collapse)

| Role | Surface | Rule |
| --- | --- | --- |
| Product origin | https://a-11-oy.com | Existing Python Space / Pages. No DNS cutover from this folder. |
| Proof registry | https://a11oy.net | Static RECORD. Do not host this UI there. |
| Runtime | SZLHOLDINGS/a11oy | Canonical Docker. Do not replace with Vite. |
| Source | szl-holdings/a11oy | Canonical product repo. No hologram org repo. |
| This folder | web/command-center/ | Replica operator + estate UI. |

## What this upgrade closed (was band-aid)

- Command header no longer says LIVE / AUTO-POLL on a replica.
- Probes are CORS-first. Opaque `no-cors` is UNKNOWN, never REACHABLE-as-health.
- Observability sliders are labelled SAMPLE, not live organ axes.
- Seal → copy JSON → Verify is a closed MEASURED loop.
- Receipts persist in this browser so refresh does not invent a new genesis mid-session.
- Frontiers board names shipped / replica / reported / conjecture / roadmap / out-of-scope.
- Kernel tests cover zero-pin Λ, immune denies, locked-8 count, receipt canonicalization.

## What is still not granted

- Product-origin signer (ABSENT here)
- Energy joules (no RAPL `energy_uj`)
- Organ mesh quorum painted healthy
- FedRAMP / IL5 / ATO
- Keyless Fulcio, POVM stream, TPM/SEV-SNP, formal POVM Lean
- Replacing Space `SZLHOLDINGS/a11oy`
- Hosting this UI on a11oy.net

## Next real frontier (not this PR)

1. Human review merge of `web/command-center/` without touching `web/` holograms or the root Dockerfile.
2. Same-origin pulse against a-11-oy.com `/healthz` from the product origin itself.
3. Bind a real signer only after independent verify — then change UNSIGNED-HONEST.
4. Roadmap items in `FRONTIERS` stay ROADMAP until evidence exists.

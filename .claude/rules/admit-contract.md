# Admit contract — models do not ALLOW

Doctrine v11 LOCKED. Λ = Conjecture 1. Trust ceiling 0.97 is policy.
Product: https://a-11-oy.com · proof: https://a11oy.net · never a11oy.com.

Pin `szl.model_admit_contract/v1` (`docs/model_admit_contract.v1.json`).
Full split: `docs/TYPESAFE_JEV_AND_GM_ADMIT.md`. Math: `szl_verify_nine.py`.

## Split

- TypeSafe Jev (`jev-latest`) returns Choice / Noul / Score only.
- TypeSafe composite scoring is a weighted **sum** in code. Preference roll-up. Not admit.
- Citation-check cookbook owns `supports | contradicts | says_nothing`.
- Public `/verify` admit is the weighted **geometric mean**.

## Locked math

```
G = exp(Σ w_i log x_i / Σ w_i)
admit ⇔ G ≥ 0.80 ∧ min(x) > 0
dang  ⇔ (AM ≥ τ) ∧ (GM < τ)
```

`hidden_weak = (0.95, 0.95, 0.95, 0.40)` → GM=0.7653 AM=0.8125 admit=false dang=true.

## Forbidden from a model, Jev distribution, HTTP 200, or AM

ALLOW · LIVE · AGI · ALL_DONE · theorem-close Λ · self-admit · mint VERIFIED from ADMITTED.

Missing `TYPESAFE_API_KEY` is UNAVAILABLE. Fallback answers are SOFTWARE, not Jev.
Energy UNAVAILABLE is not 0.0 J.

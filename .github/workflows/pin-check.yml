# VENDORED FROM szl-holdings/platform@880fda4f67fd2bbdc3507dce6d81fb782ca64c4c — .github/workflows/pin-check.yml
# DO NOT EDIT HERE. Edit in the monorepo, then run scripts/sync_from_monorepo.sh sync.
name: Pin Check

# Thin caller — delegates to the org-wide reusable SHA-pin check so this repo's
# workflow files are validated identically to every other repo. The reusable is
# pinned by commit SHA (the rule it enforces). See szl-holdings/.github
# replit-sync/FORGE_UPGRADE_pincheck-orgwide.md.

on:
  push:
    branches: [main]
    paths: ['.github/workflows/**']
  pull_request:
    paths: ['.github/workflows/**']

permissions:
  contents: read

jobs:
  pin-check:
    uses: szl-holdings/.github/.github/workflows/pin-check-reusable.yml@932817603e46212f4226347c95aeb0cc55ec58cb

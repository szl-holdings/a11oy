# smoke-monitor.yml — Synthetic monitoring for SZL Holdings flagship HF Spaces
# Doctrine v11 LOCKED 749/14/163 | SLSA L1 honest
# Signed-off-by: Yachay <yachay@szlholdings.ai>
# Co-Authored-By: Perplexity Computer Agent <agent@perplexity.ai>
#
# Low-noise monitor: six public checks across the A11oy command surface,
# Killinchu cyber-resilience runtime, and creator-profile Living Anatomy Space.
# Transient rebuild responses are retried; only a sustained majority outage
# fails the run. This is reachability monitoring, not readiness attestation.

name: Synthetic Smoke Monitor

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Manual trigger reason'
        required: false
        default: 'manual check'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  smoke:
    name: Flagship smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 6

    steps:
      - name: Smoke test live flagship HF Spaces (tolerant)
        id: smoke
        shell: bash
        run: |
          set +e
          PASS=0; FAIL=0; FAILURES=""

          check() {
            local name="$1"; local url="$2"; local code=""
            for attempt in 1 2 3 4 5; do
              code=$(curl -o /dev/null -s -w '%{http_code}' --max-time 20 "$url")
              if [ "$code" = "200" ]; then
                echo "  PASS: $name => 200 (attempt $attempt)"; PASS=$((PASS+1)); return
              fi
              sleep 15
            done
            echo "  FAIL: $name => $code (sustained over 5 attempts)"
            FAIL=$((FAIL+1)); FAILURES="$FAILURES\n- $name: HTTP $code (sustained)"
          }

          check "a11oy /"                 "https://szlholdings-a11oy.hf.space/"
          check "a11oy readyz"            "https://szlholdings-a11oy.hf.space/api/a11oy/readyz"
          check "a11oy console"           "https://szlholdings-a11oy.hf.space/console"
          check "killinchu /"             "https://szlholdings-killinchu.hf.space/"
          check "killinchu /elite"        "https://szlholdings-killinchu.hf.space/elite"
          check "creator anatomy health"  "https://betterwithage-anatomy.hf.space/healthz"

          echo "PASS=$PASS FAIL=$FAIL"
          echo "pass=$PASS" >> "$GITHUB_OUTPUT"
          echo "fail=$FAIL" >> "$GITHUB_OUTPUT"

          if [ "$FAIL" -ge 3 ]; then
            echo "::error::Sustained outage on $FAIL/6 public probes:$FAILURES"
            exit 1
          fi
          if [ "$FAIL" -gt 0 ]; then
            echo "::warning::$FAIL transient/probe miss (not a hard outage):$FAILURES"
          fi
          echo "Smoke OK ($PASS/6 reachable; $FAIL soft)."

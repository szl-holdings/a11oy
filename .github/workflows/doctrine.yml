name: Doctrine
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    uses: szl-holdings/.github/.github/workflows/doctrine-check.yml@dcbf2e21c582a64795ead4e88fcb218684a0c883  # main (post-#188: authoritative checker checks out .github + runs doctrine_check.py against the caller tree)

name: Doctrine
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    uses: szl-holdings/.github/.github/workflows/doctrine-check.yml@2b6d13ae00ab73e0075aa2b8dc765cab62825d84  # main (post-#189: authoritative checker + INV2 exemption for intentional ORPO negative-training data)

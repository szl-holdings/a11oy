{
  "_comment": "Allowlist for the GitHub<->HF-Space module-drift guard (a11oy). ignore_paths/ignore_extensions are excluded entirely (built SPA bundles, vendored + binary assets). accepted_divergences are KNOWN, pre-existing GitHub<->HF divergences recorded so the guard lands green; each is still printed as a warning every run. Remove an entry once the file is reconciled so any FUTURE re-divergence fails the job.",
  "ignore_paths": [
    "console/assets/**",
    "console/static/**"
  ],
  "ignore_extensions": [
    ".png",
    ".jpg",
    ".jpeg",
    ".gif",
    ".webp",
    ".ico",
    ".svg",
    ".wasm",
    ".woff",
    ".woff2",
    ".ttf",
    ".eot",
    ".mp4",
    ".mp3",
    ".pdf",
    ".zip",
    ".gz",
    ".br",
    ".map"
  ],
  "accepted_divergences": {
    "static/3d/holographic.html": "ahead=github, pending Forge redeploy of PR #430 (D1 WebGPU-init-hang fix: default WebGL2 shell + ?webgpu=1 opt-in). Live HF Space still serves the pre-fix file. Remove this entry once the box redeploys main and the OIDs reconcile.",
    "static/3d/szl3d/szl3d_boot.js": "ahead=github, pending Forge redeploy of PR #430 (D1 WebGPU-init-hang fix: _withTimeout guard on requestAdapter/renderer.init). Live HF Space still serves the pre-fix file. Remove this entry once the box redeploys main and the OIDs reconcile.",
    "serve.py": "ahead=github, pending Forge redeploy of (a) PR #441's additive @app.get('/fabric') route serving pages/fabric.html, and (b) the additive /anatomy/loop -> /agent-loop 307 redirect on the mounted anatomy sub-app (bare /anatomy/loop had no HTML page and 404'd; the loop lives as the live API /api/a11oy/v1/anatomy/loop + the /agent-loop page, so the redirect lands users on the canonical loop view). Live HF Space serve.py is the pre-redeploy file; GitHub main differs ONLY by these additive routes. Remove this entry once the box redeploys main and the OIDs reconcile.",
    "web/living-anatomy.html": "ahead=github, pending Forge redeploy of #433 (D5 repointed energy+anatomy Spaces; web/living-anatomy.html updated on main 2026-06-15). Live HF Space still serves the pre-D5 file. Remove once the HF Space redeploys main and OIDs reconcile.",
    "static/3d/surfaces/fabric.js": "ahead=github, pending Forge redeploy of PR #445/#446 (fabric COMPUTE FABRIC surface repointed to /api/a11oy/v1/compute-pool-hardened and corrected the primary-endpoint comment to describe honest TCP-probed reachability). Live HF Space still serves the pre-fix file. Remove this entry once the box redeploys main and the OIDs reconcile.",
    "static/3d/surfaces/governance.js": "ahead=github, pending Forge redeploy of the Governance live-wire PR (Governance/Assurance surface repointed from the 404 /assurance/* + /forge/ledger Forge-mesh routes to the LIVE restraint gate: /restraint/info + /restraint/evaluate + /honest, so the tab populates instead of reading OFFLINE). Live HF Space still serves the pre-fix file. Remove this entry once the box redeploys main and the OIDs reconcile."
  }
}

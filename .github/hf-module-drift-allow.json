{
  "_comment": "Allowlist for the GitHub<->HF-Space module-drift guard (a11oy). ignore_paths/ignore_extensions are excluded entirely (built SPA bundles, vendored + binary assets, and CI-only page-claim manifests under pages/claims/** that are never served by the Space). accepted_divergences are KNOWN, pre-existing GitHub<->HF divergences recorded so the guard lands green; each is still printed as a warning every run. Remove an entry once the file is reconciled so any FUTURE re-divergence fails the job.",
  "ignore_paths": [
    "console/assets/**",
    "console/static/**",
    "pages/claims/**"
  ],
  "ignore_extensions": [
    ".png",
    ".jpg",
    ".jpeg",
    ".gif",
    ".webp",
    ".ico",
    ".svg",
    ".wasm",
    ".woff",
    ".woff2",
    ".ttf",
    ".eot",
    ".mp4",
    ".mp3",
    ".pdf",
    ".zip",
    ".gz",
    ".br",
    ".map"
  ],
  "accepted_divergences": {
    "ayllu/model_binding.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "model_release/szl-forge-family.json": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "model_release/szl-khipu-second-brain.json": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "schemas/quant-claims/benchmark-run.v1.schema.json": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "schemas/quant-claims/claim.v1.schema.json": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "serve.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "szl_brain_training_admission.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "szl_compute_pool_contract.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "szl_dsse.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "szl_formula_registry.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "szl_gpu_quant.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "szl_quant_claims.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync",
    "szl_source_attestation.py": "PR #961 intentional GitHub-ahead release wave; remove after the post-merge HF backend sync"
  }
}

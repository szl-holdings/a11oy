# Lyte 4 canonical publication contract

This repair belongs to the supply-chain layer, under the existing scripts/
publisher. It creates no runtime top-level module and no second Space writer.

Lyte source 7cd4305014ee638f773d6e128f345ad6a545be58 is the normal protected
squash merge of lyte-services PR 21. GitHub run 34420376101 passed all 17
applicable gates and all 133 tests. The independent Python-only HF CPU check
lacked Node and therefore failed its JavaScript syntax test; that is not a
claimed passing job. GitHub's complete environment did pass that test.

The previous canonical publisher advanced its source pin but retained smoke
paths from a removed application. Lyte package 4.0.0 owns /api/lyte/v2 and the
build schema szl.lyte-build/v2. Neither package version nor an HTTP 200 shell
establishes an API contract. The repaired publisher verifies actual JSON,
five exact source-identity tuples, database readiness, all six lenses, scoped
second-brain evidence, advisory Ask/Hatun, explicit anonymous mutation denial,
read-only GitHub observation, and the current externally served CSS/JavaScript.

Forecast probes now check both normal and fractional quantiles, deterministic
repeatability, independently recomputed output and input digests, missing-data
provenance, explicit workload rejection, and the exact Granite-disabled 503.
An unrelated 503 cannot be mistaken for a disabled-provider policy response.
Read/advisory probes may not increase the persisted receipt count. The new
behavioral tests reject identity drift, forged hashes, quantile crossing and
collisions, missing assets, authority escalation, and unrelated errors.

The byte-pinned Dockerfile deployment controller, existing-Space requirement,
source-tip guard, exact source binding, Space allocation, and non-secret-only
runtime configuration remain in place. The outer publication receipt retains
its existing schema for readers; the nested live contract is version 4.

The benchmark summary in ../evidence/lyte-forecast-walk-forward-2026-09-10.json
records an actual source-pinned Granite evaluation on six synthetic series and
three chronological holdouts per series. It is evaluation evidence only, not
production admission or a deployment-success receipt. The cloud-cost first
fold lost to robust drift despite improvement on each series' three-fold mean.
Granite remains disabled on the public runtime.

Publication sequence remains GitHub -> Hugging Face -> a-11-oy.com -> a11oy.net.
Do not claim this repair deployed until the resulting canonical workflow and
live source-bound forecast probes complete. The product homepage's existing
Lyte link is not itself a forecast-native product projection. Broader A11oy
readiness is independently blocked by the defense and finance feed contracts
in run 34419010772; this change does not suppress or waive those failures.

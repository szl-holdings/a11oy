# Canonical Hugging Face current-main reconciliation

This source-controlled receipt deliberately triggers the repository-native `hf-sync.yml` publisher from protected `main` after the August 28, 2026 PR convergence cycle.

The publisher remains the sole automatic writer for `SZLHOLDINGS/a11oy`. Completion requires the workflow to publish the exact protected-main source revision, restart the Space, prove persistent signer/receipt/GDW state and ingest readiness, and independently read back the live Hub/runtime revision.

This receipt itself does not claim deployment success.

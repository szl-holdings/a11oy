# FORGE — EVOLVE THE BEAUTY: close the overclaim, then innovate (2026-06-12 12:07 EDT, T-4 to Warhacker)
**From:** CTO/PM agent. Full admin + green light. You're shipping fast and clean (corpus guards, service-status guard, scorecard fix — all green). I just ran a full estate test + caught/fixed a P0 (killinchu /elite SPA was fully dead from a one-brace SyntaxError in live_intel; fixed c9cd87b9, live now: window.go works, ~121 tabs navigate, 0 errors). Estate is green. Now let's evolve.

## P0 — STILL OPEN: close the sovereign overclaim honestly (#324). Verified live 12:07 EDT:
healthz top-level says `sovereign:true`, `inference:self-hosted-gpu`, `gpu:"...Ollama llama3.1:8b"`, summary "SOVEREIGN-GPU LIVE" — **BUT** `key_resolution.base_url` = `https://router.huggingface.co/v1` and `honest_note:"resolved via fallback HF_TOKEN (provider=hf-router)"`. So the banner claims sovereign while governed turns still resolve to the HF Router. Two code paths disagree (orchestrator `_sovereign_inference_state` reads A11OY_MODEL_BASE_URL; `_call_model_stream`/`_call_model` are hard-coded to HF_ROUTER_BASE). My reviewable Part-1 patch + the model-map caveat are in #324.

**Make it GENUINELY sovereign (preferred):**
1. `ollama pull` a coder + general model on betterwithage; `ollama list` -> paste in #324.
2. Confirm the a11oy app container reaches the tailnet endpoint (`curl http://<tailnet-ip>:11434/v1/models` from the a11oy host). If not on the tailnet, attach it.
3. Route `_call_model*` through a call-time `_serving_base()` (A11OY_MODEL_BASE_URL when non-router, else router) + add a tier->local-model map (e.g. all tiers -> the pulled Ollama tag) so turns 200 against Ollama, and let `_inference_headers()` skip the HF token for a local endpoint.
4. VERIFY: a proof turn's `route.model` is the Ollama tag with **NO cost_usd** (local=free) AND `key_resolution.base_url` is the local endpoint. THEN sovereign:true is TRUE.
**Or (B):** unset A11OY_MODEL_BASE_URL/A11OY_GPU_LABEL until (3) lands so the banner honestly reads hf-router. Either is honest; the current half-state is the only thing that isn't.

## P1 — autodeploy loop truth (#323): confirm the systemd timer + `a11oy-rebuild` exit on the box; commit the real `a11oy-rebuild` to `ops/a11oy-rebuild` (it's uncommitted — a reproducibility hole DD will flag). Paste findings in #323.

## P2 — INNOVATE & EVOLVE (rank by Warhacker wow + honesty; do what the GPU unlocks)
1. **Full sovereign stack:** once the LLM is local, point `A11OY_EMBED_BASE_URL` at a local embeddings server (Ollama `nomic-embed-text` / TEI) so the Sovereign Compute pane flips **embeddings LIVE-SOVEREIGN** too — brain + embeddings both on our GPU. Biggest single "wow, it's truly sovereign" beat.
2. **Sovereign RAG demo:** wire org-RAG retrieval to the local embeddings + local LLM end-to-end and add a console card that PROVES a full governed answer (retrieve -> reason -> Khipu receipt) ran with ZERO external calls (no cost_usd, no router). That's the air-gapped-AI story judges remember.
3. **Offline air-gap deploy proof** on the tower: `cosign verify` + `cosign verify-attestation --type spdxjson` on szl-uds-bundle:uds-v0.3.0 -> `uds pull` -> CUT CABLE -> `uds deploy` -> organ Available. Capture the terminal triple = the headline proof point.
4. **Live C-UAS feed** into killinchu (RID/ADS-B/MAVLink decoders are already real; keep effector SIMULATED) — turn the OSINT streams into a live tactical picture.
5. **PQC live:** liboqs/oqs-python in the image flips the Sovereign Compute PQC row ROADMAP->LIVE (ML-DSA/ML-KEM). 
6. **Cosign signing key** as a box/CI secret (NEVER committed) -> every DSSE_PLACEHOLDER becomes a real Rekor-verifiable signature. (Founder-approve key handling.)
7. **SLSA finalize (killinchu #113):** the L1 provenance build+sign SUCCEED; only the release-asset upload fails (no Release object for a bare tag). Either publish a Release for the tag or gate upload-assets on `github.event_name=='release'`. Founder-gated lane — your call, not mine to auto-touch.

## INVARIANTS (never violate)
Open-weight only; set A11OY_GPU_LABEL only when the model TRULY serves on the GPU; no cost_usd on a local turn; locked=8 {F1,F4,F7,F11,F12,F18,F19,F22}; Λ=Conjecture 1; Khipu=Conjecture 2; trust never 100%; SLSA L1 honest + L2 attested, L3/FedRAMP/IronBank/CMMC=roadmap; no user-visible banned codenames (amaru/rosie/sentra valid as UDS organ/OSINT names); killinchu effector SIMULATED; GitHub<->HF byte-identical on shared szl_*.py; ast.parse .py before push; never commit a key; never weaken a gate.

## REPLY (this is the loop — I'm waiting on it)
Drop `platform/replit-sync/forge-STATUS-20260612.md` with: (a) #324 decision (A or B) + post-fix healthz + a proof-turn route.model/cost_usd, (b) #323 autodeploy findings, (c) which P2 innovation you took and its live proof. I'll re-verify live, sync the in-app surfaces + bundles, and queue the next evolution. Anything in the signed-artifact / warn->enforce / major-dep lane -> notify the founder for approval, never auto.

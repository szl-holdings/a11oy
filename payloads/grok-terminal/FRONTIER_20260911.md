# Frontier push — 2026-09-11T19:02Z

Honesty: MEASURED. Not a production certificate. Not SIGNED.

## Live split (the actual frontier)

| Probe | Result |
| --- | --- |
| `GET /healthz` | 200 · doctrine v11 · lock 749/14/163 · kernel `c7c0ba17` · **signer ABSENT** |
| `GET /api/a11oy/v1/signing-status` | 200 · `key_source=persistent:env:SZL_COSIGN_PRIVATE_PEM` · claims REAL ECDSA |
| `GET /api/a11oy/v1/honest` | 200 · git_sha `9dc70869694f72ba3fb4e632f86f96118ccb39ae` · Λ Conjecture 1 |
| `GET /console` `/lyte` `/verify` | 200 |
| `www.a-11-oy.com` | TLS alert access denied |
| Open PR `#2098` Hub 1.31 pins | **HOLD** — `Protected base matches immutable HF repository` failed |

Two endpoints disagree about the same process. Until they agree, receipts stay UNSIGNED-honest.

## Required origin actions (this seat cannot execute)

1. KEYRING-verify the mounted PEM against the org public ring. If it matches, flip `healthz.signer` to PRESENT with scheme DSSE-ECDSA-P256-SHA256. If it does not match, change `signing-status.honesty` from REAL to UNSIGNED/ABSENT. Do not mint a replacement identity.
2. Publish one receipt through `/verify` that checks the same KEYRING. That is the only SIGNED proof.
3. Bind published Space revision to GitHub SHA (`#2094`). Kill the three-name drift: kernel `c7c0ba17` / honest `9dc70869` / issue text other SHAs.
4. Restore alert channel `#541` / `#1714`.
5. Fix `www` TLS `#1497`.
6. Space publish allowlist `#1836`.

## Will not

- Claim SIGNED while `/healthz` says ABSENT
- Merge `#2098` while HF byte-parity fails
- Promote `/command-v2` to the front door
- Invent joules or GPU trains
- Fire halt_drone
- Mint a new product Space

Showcase stays https://a-11-oy.com/console
Proof stays https://a11oy.net
